# Real-Time Emotion Recognition (VS Code Ready)

## Steps to Run

1. Open this folder in VS Code
2. Create & activate virtual environment
3. Install dependencies:
   python -m pip install -r requirements.txt
4. Add emotion images to dataset/train folders
5. Train model:
   python train.py
6. Run webcam:
   python camera.py

Press Q to quit webcam.
